# Streamlit Portfolio Generator Requirements
# 포트폴리오 생성기를 위한 필수 패키지들

# 웹 애플리케이션 프레임워크
streamlit>=1.28.0

# 파일 처리 및 압축
zipfile36>=0.1.3

# 날짜/시간 처리 (표준 라이브러리이지만 명시)
datetime 

# 파일 시스템 작업 (표준 라이브러리이지만 명시)
os 

# 선택적 패키지들 (고급 기능용)
# 아래는 필요시 주석 해제하여 사용

# 템플릿 엔진 (더 복잡한 HTML 생성용)
jinja2>=3.1.0

# 마크다운 처리
markdown>=3.5.0

# HTML 파싱/조작
beautifulsoup4>=4.12.0

# PDF 변환 (HTML to PDF)
weasyprint>=60.0

# 웹 요청
requests>=2.31.0

# 이미지 처리
Pillow>=10.0.0

# 데이터 처리
pandas>=2.0.0

# 차트/그래프
plotly>=5.17.0
matplotlib>=3.7.0
